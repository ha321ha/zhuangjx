## 关闭系统的"应用执行别名"（会跳转到微软商店的假快捷方式）

1. 按 `Win + I` 打开 Windows 设置

2. 点击 **"应用"** → **"高级应用设置"** → **"应用执行别名"**

3. 找到 **"Python"** 和 **"Python3"**，把开关 **关掉**

关掉之后，再敲 `python3` 就会正常指向你装好的 Python 了。



## 想用 `python3` 命令

打开你 Python 的安装目录，`python.exe` 文件，重命名为 `python3.exe`
